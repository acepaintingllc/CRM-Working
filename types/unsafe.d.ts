// Central escape hatch for dynamic payloads while we migrate to strict models.
// Replace `Unsafe` usage with concrete types module-by-module.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
type Unsafe = any;
